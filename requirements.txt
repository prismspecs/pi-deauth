# WiFi Deauth Wardrive - Python Dependencies
# Install all dependencies with: sudo bash install.sh
# Or manually: pip3 install -r requirements.txt

# LCD Display Support (16x2 I2C LCD)
RPLCD>=1.3.0

# Note: System packages must be installed via apt (handled by install.sh):
#   - aircrack-ng (airodump-ng, aireplay-ng, airmon-ng)
#   - python3-smbus (I2C support for LCD)
#   - i2c-tools (I2C debugging)
#   - firmware-ralink (for RT5572/Panda PAU09)
#   - firmware-realtek (for RTL8187/Alfa adapters)
#   - firmware-atheros (for Atheros chipsets)

