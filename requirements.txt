# WiFi Deauth Wardrive - Python Dependencies
# Install with: pip3 install -r requirements.txt

# LCD Display Support (16x2 I2C LCD)
RPLCD==1.3.1

# Note: The aircrack-ng suite must be installed via apt, not pip:
#   sudo apt install aircrack-ng python3 python3-smbus i2c-tools

